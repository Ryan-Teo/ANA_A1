import java.io.PrintWriter;

public class matrixTest {
	AdjMatrix adjM = new AdjMatrix();
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		PrintWriter os = new PrintWriter(System.out, true);
	}
	
	AdjMatrix()
	addVertex(T)
	addEdge(T, T)
	neighbours(T)
	removeVertex(T)
	removeEdge(T, T)
	printVertices(PrintWriter)
	printEdges(PrintWriter)
	shortestPathDistance(T, T)

}
